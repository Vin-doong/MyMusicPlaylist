<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Top 100 플레이리스트</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" href="/favicon_io/favicon.ico" type="image/x-icon">
  <link rel="stylesheet" href="/css/top100.css"> <!-- 추가된 CSS 파일 -->
</head>
<body>
  
  <h1>Top 100 플레이리스트</h1>
  <p>Spotify 플레이어에서 바로 듣기</p>

  <div class="slider-container">
    <div class="slider-track">
      <div class="slider-item" data-bg="#FFDDC1">
        <iframe
          src="https://open.spotify.com/embed/playlist/4cRo44TavIHN54w46OqRVc"
          frameborder="0"
          allowtransparency="true"
          allow="encrypted-media">
        </iframe>
      </div>
      <div class="slider-item" data-bg="#C1E1FF">
        <iframe
          src="https://open.spotify.com/embed/playlist/37i9dQZEVXbMDoHDwVN2tF"
          frameborder="0"
          allowtransparency="true"
          allow="encrypted-media">
        </iframe>
      </div>
      <div class="slider-item" data-bg="#FFC1E3">
        <iframe
          src="https://open.spotify.com/embed/playlist/37i9dQZEVXbNxXF4SkHj9F"
          frameborder="0"
          allowtransparency="true"
          allow="encrypted-media">
        </iframe>
      </div>
      <div class="slider-item" data-bg="#C1FFD7">
        <iframe
          src="https://open.spotify.com/embed/playlist/37i9dQZF1DXcBWIGoYBM5M"
          frameborder="0"
          allowtransparency="true"
          allow="encrypted-media">
        </iframe>
      </div>
    </div>
    <!-- Navigation Buttons -->
    <button class="slider-button left" id="prevButton">
      <img src="https://img.icons8.com/ios-filled/50/ffffff/chevron-left.png" alt="Previous">
    </button>
    <button class="slider-button right" id="nextButton">
      <img src="https://img.icons8.com/ios-filled/50/ffffff/chevron-right.png" alt="Next">
    </button>
  </div>
  <!-- 플레이어 푸터 추가 -->
  <div id="playerFooterContainer" class="footer-container">
    <%- include('partials/playerFooter'); %>
  </div>

  <script>
    const track = document.querySelector('.slider-track');
    const prevButton = document.getElementById('prevButton');
    const nextButton = document.getElementById('nextButton');
    const body = document.body;
    const sliderItems = document.querySelectorAll('.slider-item');

    let currentIndex = 0;
    const totalItems = sliderItems.length;

    function updateSliderPosition() {
      const offset = -currentIndex * 100; // Move slider by percentage
      track.style.transform = `translateX(${offset}%)`;

      // Update background color
      const currentBg = sliderItems[currentIndex].getAttribute('data-bg');
      body.style.backgroundColor = currentBg;

      updateButtonState();
    }

    function updateButtonState() {
      prevButton.disabled = currentIndex === 0;
      nextButton.disabled = currentIndex === totalItems - 1;
    }

    prevButton.addEventListener('click', () => {
      if (currentIndex > 0) {
        currentIndex--;
        updateSliderPosition();
      }
    });

    nextButton.addEventListener('click', () => {
      if (currentIndex < totalItems - 1) {
        currentIndex++;
        updateSliderPosition();
      }
    });

    // 초기 상태 설정
    updateSliderPosition();
  </script>
  
</body>
</html>
