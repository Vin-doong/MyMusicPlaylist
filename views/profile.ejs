<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>회원정보</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" href="/favicon_io/favicon.ico" type="image/x-icon">
  <link rel="stylesheet" href="/css/profile.css">
</head>
<body>
  <%- include('partials/header') %>

  <div class="container mt-5">
    <h1 class="text-center">회원정보</h1>
    <div class="card mx-auto shadow-sm" style="max-width: 500px;">
      <div class="card-body text-center">
        <img 
          src="<%= user?.images?.[0]?.url || '/default-profile.png' %>" 
          alt="프로필 이미지" 
          class="profile-image"
        >
        <h5 class="card-title"><%= user?.display_name || '이름 정보 없음' %></h5>
        <p class="card-text card-text-muted">이메일: <%= user?.email || '이메일 정보 없음' %></p>
        <p class="card-text card-text-muted">팔로워: <%= user?.followers?.total || 0 %></p>
        <p class="card-text card-text-muted">Spotify ID: <%= user?.id || 'ID 정보 없음' %></p>
        <a href="/main" class="btn btn-secondary mt-3">홈으로</a>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
